#include<stdio.h>

int main() {

    // void printCompleteNumbers(int number) {
    //     int i;
    //     int reminder = number;

    //     for (i = number / 2; i >= 1; i--) {
    //         if (number % i == 0) {
    //             reminder -= i;
    //         };
    //     };

    //     if (reminder == 0) {
    //         for (i = number / 2; i >= 1; i--) {
    //             if (number % i == 0) {
    //                 printf("%d\n", i);
    //             };
    //         };
    //     };
    // };

    // =============================================================================
};

#include<stdio.h>

int main() {

    // int convertBinaryToDecimal(int number) {
    //     if (number <= 0) return 0;
    //     return (number % 10) + 2 * convertBinaryToDecimal(number / 10);
    // };

    // printf("%d\n", convertBinaryToDecimal(110));

    // ============================================================================= laboratory 7.1

    // int convertDecimalToBinary(int number) {
    //     if (number <= 0) return 0;
    //     return (number % 2) + 10 * convertDecimalToBinary(number / 2);
    // };

    // printf("%d\n", convertDecimalToBinary(2));

    // ============================================================================= laboratory 7.2

    // int rec(int number, int divider) {
    //     if (divider == 0 || number == 0) return 0;
    //     else if (number < 0 && divider < 0 && divider < number) return 0;
    //     else if (number > 0 && divider > 0 && divider > number) return 0;
    //     return rec(number - (divider), divider) + 1;
    // };

    // int rem(int number, int divider) {
    //     if (divider == 0 || number == 0) return number;
    //     else if (number < 0 && divider < 0 && divider < number) return number;
    //     else if (number > 0 && divider > 0 && divider > number) return number;
    //     return rec(number - (divider), divider);
    // };

    // printf("%d\n", rem(10, 6));

    // printf("%d\n", 11 % 3);

    // ============================================================================= laboratory 7.3

    return 0;
};
